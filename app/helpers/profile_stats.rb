#helpers do


    # if current_user
    #   rounds = Rounds.where(user_id: current_user.id)
    #   rounds.each do |round|
    #     puts "#{round.deck.name}"
    #     # deck = Decks.where(id: round.deck_id)
    #     count = Card.where(deck_id: round.deck_id).count
    #     puts "#{round.timestamp}"
    #     puts "first guess correct: #{round.result}"
    #     puts "Total guesses: #{round.total_result}"
    #   end
    # end
#   end
# end

